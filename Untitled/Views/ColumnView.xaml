<UserControl x:Class="Untitled.ColumnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:untitled="clr-namespace:Untitled"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200">
    
    <DockPanel MinWidth="64">
        <ComboBox DockPanel.Dock="Top">
            <ComboBoxItem/>
        </ComboBox>
        <ScrollViewer DockPanel.Dock="Top">
            <ListView x:Name="fsNodesList" ItemsSource="{Binding FsNodes}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <DockPanel x:Name="viewRoot" Margin="0,2,0,2" Height="20">
                            <Image DockPanel.Dock="Left" x:Name="icon" Width="16" Height="16" Source="/StaticResources/Textfile_818_16x.png" />
                            <TextBlock DockPanel.Dock="Left" x:Name="name" Margin="0,1,0,0" Text="{Binding FsNode}"></TextBlock>
                        </DockPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <!--<ListView x:Name="fsNodesList" SelectionMode="Extended" ItemsSource="{Binding FsNodes}">
            </ListView>-->
        </ScrollViewer>
    </DockPanel>
    
</UserControl>
