<UserControl x:Class="Untitled.ColumnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:untitled="clr-namespace:Untitled"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200">
    
    <DockPanel x:Name="columnViewRoot" MinWidth="64" Height="300">
        <ComboBox x:Name="parentFsNodesComboBox" DockPanel.Dock="Top">
            <ComboBox.ItemsSource>
                <Binding Path="ParentFsNodesViews"/>
            </ComboBox.ItemsSource>
        </ComboBox>
        <!--<ScrollViewer x:Name="childFsNodesScroller" DockPanel.Dock="Top">-->
            <ListView x:Name="childFsNodesListView" ScrollViewer.VerticalScrollBarVisibility="Visible">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <EventSetter Event="MouseDoubleClick" Handler="listViewItem_MouseDoubleClick" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemsSource>
                    <Binding Path="ChildFsNodesViews" />
                </ListView.ItemsSource>
            </ListView>
        <!--</ScrollViewer>-->
    </DockPanel>
    
</UserControl>
